METODOS:

-instancia le pertenece a la instancia y cada instancia tiene un valor diferente
-Clase: le pertenece a la clase, es el mismo para todas las instancias =Static



final sirve para no modificar 

constructor inicializador de variables 
debe tener el mismo nombre de la clase 
clase dentro de otra se le llama composicion 

agregra estos metodos :
     addStakeHolder
     calculateFunds()  --setfunds
     removeStakeHolder()
     viewStakeHolder()
